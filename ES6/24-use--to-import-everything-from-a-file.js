// The code below requires the contents of a file, "capitalize_strings", found in the 
// same directory as it, imported. Add the appropriate import * statement to the top 
// of the file, using the object provided.

import * as stringsFile from "capitalize_strings";
"use strict";